function App() {
  return (
    <main style={{ padding: "2rem", fontFamily: "system-ui" }}>
      <h1>React + Vite + PWA</h1>
      <p>PWA is configured and ready.</p>
    </main>
  );
}

export default App;
